{
  "name": "casakira",
  "version": "0.1.0",
  "private": true,
  "description": "Casa Kira — industrial sewing machine dealer website (Buenos Aires, since 1968)",
  "scripts": {
    "build": "node build.js && node check.js",
    "check": "node check.js",
    "bundle": "node build-single-file.js"
  },
  "engines": {
    "node": ">=18"
  }
}
